const STORE_SLICES = {
  AUTH: "auth",
  SPACELIST: "spaceList",
};

const PERSIST_KEYS = {
  AUTH: "auth",
  SPACELIST: "spaceList",
};

const QUERY_KEYS = {
  SPACELIST: "spaceList",
  SINGLESPACELIST: "singleSpaceList",
  PROFILE: "profile",
};

export { STORE_SLICES, PERSIST_KEYS, QUERY_KEYS };
